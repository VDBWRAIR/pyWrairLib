#!/usr/bin/env python

import sys
from aceread import Ace

if len( sys.argv ) != 3:
    print "Usage: %s <ace filepath> <contigname>" % __file__
    sys.exit( -1 )

for read in Ace( sys.argv[1] ).reads_for_contig( sys.argv[2] ):
    print read
